# User Timezone Preference in DB

Store the user's preferred timezone in the database so the training calendar can correctly render dates and timestamps server-side and persist the setting across devices.
